
<h4>
  Add Review
</h4>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating, placeholder: 'Input rating 1-5' %>
  <%= f.input :content %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
